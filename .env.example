PORT=4000

# Option A: single URL (common on Railway, Render, Fly, etc.)
# DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DATABASE

# Option B: separate fields (used if DATABASE_URL is unset)
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=seoi
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
